import 'package:flutter/material.dart';

class AttendanceShift extends StatelessWidget {
  const AttendanceShift({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Attendance Shift'),
      ),
    );
  }
}