import 'package:flutter/material.dart';

class AttendanceSquare extends StatelessWidget {
  const AttendanceSquare({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Attendance Square'),
      ),
    );
  }
}
